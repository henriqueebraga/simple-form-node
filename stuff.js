let counter = (array) => {
    return `this array has ${array.length} elements `
};

let adder = (a,b) => {
    return `this total is ${a+b}`;
};

let pi = 3.142;

module.exports = {
    counter,
    adder,
    pi
}

